# Base dependencies — installed by launch scripts on first run.
# Heavy ML packages (torch, torchaudio, whisperx, pyannote) are NOT listed here;
# they are installed dynamically by app/services/pip_installer.py during
# onboarding. Packages only some models need — gliner (PII), mlx-whisper (Apple
# Silicon) — are installed on demand from MODEL_OPTIONAL_PACKAGES in the same
# module, so an install that never uses those models never pays for them.
# See requirements-lock.txt for the full tree.
#
# Versions are PINNED (==) so a fresh install months from now reproduces a
# known-good base layer instead of resolving whatever is newest on PyPI.
# Keep these in sync with the matching lines in requirements-lock.txt.
# Test runner and linter are NOT here — a user's install carries no dev tooling;
# install pytest and ruff by hand in the venv when working on the code.
# To regenerate the lock file after changing this file or completing onboarding:
#   pip freeze > backend/requirements-lock.txt
flask==3.1.3
flask-sqlalchemy==3.1.1
flask-socketio==5.6.1
flask-cors==6.0.5
huggingface-hub==0.36.2
psutil==7.2.2
static-ffmpeg==3.0
